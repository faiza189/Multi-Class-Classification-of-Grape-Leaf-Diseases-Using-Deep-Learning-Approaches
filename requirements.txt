# Grape Leaf Disease Classification 
# Python 3.10 or newer. Install with:  pip install -r requirements.txt
#
# The saved notebook run used TensorFlow 2.20.0 on a Google Colab GPU runtime.
# On Colab most of these are preinstalled and only `kaggle` usually needs a
# fresh install, so this file mainly matters for running the notebook locally.

# Deep learning
tensorflow==2.20.0            # use tensorflow[and-cuda]==2.20.0 for local GPU support

# Data handling
numpy>=1.26,<3.0
pandas>=2.0

# Image loading
pillow>=10.0

# Plotting
matplotlib>=3.8
seaborn>=0.13

# Metrics, splits, label binarisation, ROC curves
scikit-learn>=1.4

# Dataset download from Kaggle (needs ~/.kaggle/kaggle.json with chmod 600)
kaggle>=1.6
